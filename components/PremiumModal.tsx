import React from 'react';

const PremiumModal: React.FC = () => {
  // This component is not currently used but is being kept for potential future use.
  // It was previously an empty file, causing a fatal error on application load.
  return null;
};

export default PremiumModal;
